{% extends 'base.html.twig' %}

{% block body %}
    <h1>Ticket edit</h1>

    <div class="container">

        {{ form_start(edit_form) }}
        <div class="form-group row">
            {{ form_widget(edit_form) }}
            <input type="submit" value="Edit" class="btn btn-primary">
            {{ form_end(edit_form) }}
        </div>
    </div>
    <hr>

    <a href="{{ path('ticket_show', { 'id' : ticket.id}) }}" class="btn btn-primary">Back to the ticket</a>
{% endblock %}
